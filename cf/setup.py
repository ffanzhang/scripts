from setuptools import setup

setup(name='cflib',
            version='0.1',
            description='helper methods submitting to codeforces',
            install_requires=['requests'],
            license='MIT',
            packages=['cflib'],
            zip_safe=False)
